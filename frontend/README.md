# LedgerFlow Dashboard (Frontend)

React + Vite + TypeScript fintech dashboard frontend for LedgerFlow backend.

## Run
```bash
npm install
npm run dev
```

Set API URL:
```bash
VITE_API_URL=http://localhost:8080
```
